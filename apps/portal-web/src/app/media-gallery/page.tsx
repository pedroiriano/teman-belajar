/* eslint-disable @next/next/no-img-element -- public media uses the authenticated, policy-enforced media route */
import type {Metadata} from "next";
import Link from "next/link";
import {EmptyState,ErrorState,PageHero} from "@/components/public-content";
import {listMediaCollections,type MediaCollectionKind} from "@/lib/media-gallery";

export const metadata:Metadata={title:"Galeri Media & Video Hub",description:"Galeri foto dan video pembelajaran terkurasi dari Teman Belajar.",alternates:{canonical:"/media-gallery"}};
const labels:Record<MediaCollectionKind,string>={image_gallery:"Galeri foto",video_hub:"Video Hub"};
function href(query:string,kind:string,page:number){const params=new URLSearchParams();if(query)params.set("q",query);if(kind)params.set("kind",kind);if(page>1)params.set("page",String(page));return `/media-gallery${params.size?`?${params}`:""}`}
export default async function MediaGalleryPage({searchParams}:{searchParams:Promise<{q?:string;kind?:string;page?:string}>}){const raw=await searchParams;const query=(raw.q||"").trim().slice(0,100);const kind=["image_gallery","video_hub"].includes(raw.kind||"")?raw.kind||"":"";const page=Math.max(1,Number.parseInt(raw.page||"1",10)||1);const result=await listMediaCollections(query,kind,page);return <div><PageHero eyebrow="Galeri terkurasi" title="Foto dan video untuk memperkaya pembelajaran" description="Jelajahi koleksi media yang telah ditinjau dan diterbitkan. Storage mentah dan aset draf tidak pernah ditampilkan." icon="gallery"/><section className="portal-container py-10 sm:py-14"><form role="search" action="/media-gallery" className="portal-card grid gap-4 p-5 sm:grid-cols-[minmax(0,1fr)_12rem_auto] sm:items-end"><div><label className="portal-label" htmlFor="gallery-search">Cari koleksi</label><input id="gallery-search" name="q" type="search" maxLength={100} defaultValue={query} className="portal-search-input mt-2 w-full"/></div><div><label className="portal-label" htmlFor="gallery-kind">Jenis</label><select id="gallery-kind" name="kind" defaultValue={kind} className="portal-control mt-2 w-full"><option value="">Semua jenis</option><option value="image_gallery">Galeri foto</option><option value="video_hub">Video Hub</option></select></div><button className="portal-button-primary">Terapkan</button></form>{result.error?<div className="mt-8"><ErrorState title="Galeri belum dapat dimuat"/></div>:result.data.length===0?<div className="mt-8"><EmptyState title="Belum ada koleksi terbit" description="Koleksi yang telah melewati peninjauan akan tampil di sini."/></div>:<><div className="mb-6 mt-10 flex items-end justify-between"><div><p className="portal-eyebrow">Koleksi publik</p><h2 className="mt-2 text-2xl font-black text-slate-900">Media pilihan</h2></div><p className="text-sm font-bold text-slate-500">{result.total} koleksi</p></div><div className="grid gap-6 md:grid-cols-2 xl:grid-cols-3">{result.data.map(collection=>{const cover=collection.items.find(item=>item.featured)||collection.items[0];return <article key={collection.id} className="portal-card overflow-hidden">{cover?<div className="flex aspect-video items-center justify-center overflow-hidden bg-gradient-to-br from-teal-700 to-sky-700">{collection.kind==="image_gallery"?<img src={`/media/${cover.media_id}`} alt="" className="h-full w-full object-cover"/>:<span className="text-lg font-black text-white">Putar Video</span>}</div>:<div className="flex aspect-video items-center justify-center bg-slate-100 text-sm font-bold text-slate-500">Media tidak tersedia</div>}<div className="p-6"><div className="flex gap-2"><span className="portal-badge">{labels[collection.kind]}</span>{collection.featured?<span className="portal-badge">Unggulan</span>:null}</div><h3 className="mt-4 text-xl font-black text-slate-900"><Link href={`/media-gallery/${collection.slug}`} className="hover:text-teal-700">{collection.title}</Link></h3><p className="mt-3 line-clamp-3 text-sm leading-6 text-slate-600">{collection.summary}</p><Link href={`/media-gallery/${collection.slug}`} className="mt-5 inline-block font-extrabold text-teal-700">Lihat koleksi →</Link></div></article>})}</div>{result.total_pages>1?<nav className="mt-10 flex items-center justify-between" aria-label="Paginasi koleksi"><Link className={`portal-button-secondary ${page<=1?"pointer-events-none opacity-40":""}`} aria-disabled={page<=1} href={href(query,kind,Math.max(1,page-1))}>← Sebelumnya</Link><span className="text-sm font-bold text-slate-500">Halaman {page} dari {result.total_pages}</span><Link className={`portal-button-secondary ${page>=result.total_pages?"pointer-events-none opacity-40":""}`} aria-disabled={page>=result.total_pages} href={href(query,kind,Math.min(result.total_pages,page+1))}>Berikutnya →</Link></nav>:null}</>}</section></div>}
