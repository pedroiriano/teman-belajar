import { NextRequest, NextResponse } from "next/server";
import { getServerAccessToken } from "@/lib/server-auth";
export async function POST(request:NextRequest,context:{params:Promise<{id:string}>}){const token=await getServerAccessToken();if(!token)return NextResponse.json({title:"Unauthorized"},{status:401});const{id}=await context.params;if(!/^[0-9a-f-]{36}$/i.test(id))return NextResponse.json({title:"ID tidak valid"},{status:400});try{const body=await request.text();if(body.length>131072)return NextResponse.json({title:"Payload terlalu besar"},{status:413});const upstream=await fetch(`${process.env.PORTAL_API_INTERNAL_URL||"http://api:8080"}/api/v1/admin/media-collections/${encodeURIComponent(id)}/transition`,{method:"POST",headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json"},body,cache:"no-store",signal:AbortSignal.timeout(8_000)});return new NextResponse(await upstream.text(),{status:upstream.status,headers:{"Content-Type":upstream.headers.get("Content-Type")||"application/json","Cache-Control":"private, no-store"}})}catch{return NextResponse.json({title:"Transisi galeri belum tersedia"},{status:503})}}
